# Local settings (may contain API keys)
.claude/settings.local.json

# OS files
.DS_Store
*/.DS_Store

# Claude worktrees and ephemeral data
.claude/worktrees/
.claude/agent-memory/

# Plans - tracked for project optimization history

# Update session state (ephemeral, used for --resume)
.update-session.json

# Archive
.archive/
